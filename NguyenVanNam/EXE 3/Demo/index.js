/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import nvn from './nvn';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => nvn);
